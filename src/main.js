define(function (require) {$(document).ready(function() {
    console.log('start');
    
    (function() {
        const game_cfg = require('game');
        /*let*/ g_game = new Phaser.Game(game_cfg);
    })();
    
});});
